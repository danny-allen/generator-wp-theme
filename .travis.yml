language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-fed-up' ]; then cd .. && eval "mv $currentfolder generator-fed-up" && cd generator-fed-up; fi
