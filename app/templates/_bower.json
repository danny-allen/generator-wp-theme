{
    "name": "<%= _.slugify(appName) %>",
    "version": "1.0.0",
    "dependencies": <%= JSON.stringify(dependencies) %>,
    "devDependencies": {
    	"beartrap": "https://github.com/danny-allen/bear-trap.git"
	}
}